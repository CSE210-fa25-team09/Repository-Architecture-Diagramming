/**
 * Shared Prettier configuration so every teammate formats UI code consistently.
 * @type {import("prettier").Config}
 */
const config = {
  semi: false,
  singleQuote: false,
  trailingComma: "all",
  printWidth: 90,
  tabWidth: 2,
};

export default config;
